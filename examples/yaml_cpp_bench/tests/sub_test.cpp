// Generated by SDK Test Forge — edit AGENT/TODO sections for complex APIs.
#include <gtest/gtest.h>
#include "api.hpp"

TEST(Sub, Normal) {
    EXPECT_EQ(my_sdk::sub(2, 3), -1);
}

TEST(Sub, Boundary) {
    EXPECT_EQ(my_sdk::sub(0, 0), 0);
    EXPECT_EQ(my_sdk::sub(-1, 1), -2);
}

TEST(Sub, Overflow) {
    EXPECT_EQ(my_sdk::sub(2147483647, 1), my_sdk::sub(2147483647, 1));
}

TEST(Sub, Error) {
    // AGENT: fill — invalid input / error return path
    // my_sdk::sub(/* args */);
    SUCCEED();
}

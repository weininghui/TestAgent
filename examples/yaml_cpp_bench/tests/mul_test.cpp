// Generated by SDK Test Forge — edit AGENT/TODO sections for complex APIs.
#include <gtest/gtest.h>
#include "api.hpp"

TEST(Mul, Normal) {
    EXPECT_EQ(my_sdk::mul(2, 3), 6);
}

TEST(Mul, Boundary) {
    EXPECT_EQ(my_sdk::mul(0, 0), 0);
    EXPECT_EQ(my_sdk::mul(-1, 1), -1);
}

TEST(Mul, Overflow) {
    EXPECT_EQ(my_sdk::mul(2147483647, 1), my_sdk::mul(2147483647, 1));
}

TEST(Mul, Error) {
    // AGENT: fill — invalid input / error return path
    // my_sdk::mul(/* args */);
    SUCCEED();
}

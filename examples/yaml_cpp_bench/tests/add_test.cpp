// Generated by SDK Test Forge — edit AGENT/TODO sections for complex APIs.
#include <gtest/gtest.h>
#include "api.hpp"

TEST(Add, Normal) {
    EXPECT_EQ(my_sdk::add(2, 3), 5);
}

TEST(Add, Boundary) {
    EXPECT_EQ(my_sdk::add(0, 0), 0);
    EXPECT_EQ(my_sdk::add(-1, 1), 0);
}

TEST(Add, Overflow) {
    EXPECT_EQ(my_sdk::add(2147483647, 1), my_sdk::add(2147483647, 1));
}

TEST(Add, Error) {
    // AGENT: fill — invalid input / error return path
    // my_sdk::add(/* args */);
    SUCCEED();
}
